<form method="post" action="{{ path('app_push_notification_delete', {'id': push_notification.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ push_notification.id) }}">
    <button class="btn">Delete</button>
</form>
