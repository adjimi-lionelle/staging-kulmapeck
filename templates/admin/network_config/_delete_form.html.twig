<form method="post" action="{{ path('app_admin_network_config_delete', {'id': network_config.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ network_config.id) }}">
    <button class="btn btn-danger-soft">Delete</button>
</form>
