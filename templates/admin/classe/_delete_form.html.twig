<form method="post" style="display: inline;" action="{{ path('app_admin_classe_delete', {'id': classe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classe.id) }}">
    <button class="btn btn-danger-soft btn-sm">Delete</button>
</form>
