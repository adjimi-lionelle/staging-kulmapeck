<!DOCTYPE html>

<title>New FAQ</title>

{% block body %}
    <h1>Create new FAQ</h1>

    {{ include('faq/_form.html.twig') }}

    <a href="{{ path('app_f_a_q_index') }}">back to list</a>
{% endblock %}
